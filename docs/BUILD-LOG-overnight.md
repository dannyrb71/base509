# PetAppro Website — Overnight Build Log

Date: 2026-08-14  
Scope: `apps/web` marketing site and exploratory provider portal  
Working-tree only: no commit, push, or deploy

## Sources Used

- Build spec: `docs/specs/petappro-website-build-spec.md`
- Layout: Figma file `JwMhGwZg5cv3bGAP7306tj`, using every desktop/mobile frame ID listed in the spec
- Marketing copy: `apps/web/copy/petappro-site-copy.md`
- Copy guardrails and later locked decisions: `apps/web/copy/COPY-AUDIT.md`
- Code tokens and responsive typography: `apps/web/src/styles/brand-petappro.css`

## Working Decisions

1. Figma controls section order, responsive composition, imagery, and visual hierarchy. The copy document controls the words.
2. Where the July 18 copy document conflicts with later locked decisions in `COPY-AUDIT.md`, the later audit wins. Known examples: no “Free forever,” no “No per-staff fees,” GPS ships at launch rather than v1.1, SMS is post-MVP, and “Get early access” is the pre-launch primary CTA.
3. Danny’s later visual corrections override older Figma states: mobile hamburger navigation, Brand Accent instead of Hollywood Olive, Brandy Blue 800 section backgrounds, Brandy Blue 900 footer, 600-weight buttons, responsive headline breaks, and the mobile scroll-to-top control.
4. Legal bodies remain untouched. The PetAppro policy index links to Base509 policy URLs rather than creating duplicate PetAppro legal pages.
5. The provider portal is explicitly exploratory and unconnected to auth, Supabase, Stripe, or production data. Its local interactions and downloads are functional prototypes only.

## Page Status

| Route | Desktop frame | Mobile frame | Status | Notes |
|---|---:|---:|---|---|
| `/petappro/features` | `2:19` | `300:3432` | Complete | Current Figma hero, four-screen interactive carousel, six feature cards, six essentials, and exact Figma graphics. |
| `/petappro/pricing` | `2:36` | `300:3433` | Complete | Six current plan cards, annual pricing, Duo payment marker, feature matrix, honest section, FAQ, and launch-status footnote. |
| `/petappro/themes` | `2:53` | `300:3434` | Complete | Figma hero composition plus an interactive ten-theme Light/Dark preview based on the later Figma Make direction. |
| `/petappro/download` | `2:70` | `300:3435` | Complete | Store/device graphics, interactive Provider/Client screen tabs, launch notification form, and final CTA. |
| `/petappro/support` | `2:87` | `300:3436` | Complete | Four topic-level accordions containing all 29 expanded/static Q&As, plus the contact CTA. |
| `/petappro/contact` | `2:104` | `300:3437` | Complete | Contact card order and wording reconciled to the canonical source and copy audit. |
| `/petappro/signup` | `2:121` | `300:3438` | Complete | Waitlist mode per D-061; no live portal handoff or false account creation. |
| `/petappro/policies` | `2:138` | n/a | Complete | Responsive policy index only; cards point to canonical Base509 policy URLs. |
| `/portal/*` | Exploratory | Exploratory | Complete | Seven interactive provider admin/account concepts, clearly labeled and isolated from the marketing routes. |

## Marketing Page Notes

### Site-Wide Styling Pass

- Public marketing-page text now defaults to the design system's primary text token. Accent-green text is reserved for eyebrows, while text on dark surfaces is forced to the inverse/white token.
- White cards that sit on white sections now use `var(--pa-brandy-50)`. Cards on dark sections retain a light card surface with default text where the Figma composition calls for contrast.
- Footer copy and links use inverse/white text on the Brandy 900 footer surface.

### Features

- Replaced the older placeholder page with the current Figma structure and copy: Star Features, Full List, The Essentials, and the final CTA.
- Built the hero as a four-screen carousel using the exact Clear Your Day, Pet Details, Business Growth, and Data Export graphics. It auto-rotates, supports horizontal swipe and arrow keys, and exposes four direct-navigation pagination buttons.
- Pagination buttons now isolate their pointer events from the swipe surface, so selecting dot 3 reliably selects screen 3. Their width, opacity, and scale animate for 300 ms as they enter or leave the active state, with a reduced-motion override.
- All four media stages use the tallest/fourth screen's aspect ratio, align to the same left edge and width as the copy, and bottom-align each graphic. Reserved copy height keeps the pagination row from shifting between slides on mobile and desktop.
- Downloaded and used the six exact feature-card icons from Figma. Kept the illustrative-screen disclaimer with the carousel.
- Forced the hero headline to wrap before “the way you do” and the Private Notes card title to wrap before “just for you” at every viewport size. Corrected the Share the Care SVG to preserve its intrinsic aspect ratio instead of stretching vertically.
- Corrected the Essentials icon proportions to match Figma: 40px Brand Accent circles with 20px aspect-ratio-preserving icons.
- Audited every paginated carousel in `apps/web`. The shared PetAppro/Base509 home-page card carousel now matches the Features carousel: every dot is a direct navigation button, pointer-safe, and animated between inactive and active states over 300 ms, with reduced-motion support.

### Pricing

- Rebuilt the plan presentation from the current Starter, Solo, Duo, Crew, Team, and Enterprise data source.
- The H1 follows the Figma wording, “Start free. Grow predictably.” The older copy-document phrase “No per-staff fees” was not used because it conflicts with current seat-based tiers and is explicitly prohibited by the copy audit.
- Starter is a free on-ramp, not “free forever.” Duo is where in-app payments unlock. Crew adds the full theme library, GPS walk tracking, and co-branding removal.
- Removed SMS alerts from the feature matrix and footnote. Starter inclusions remain accessible to screen readers but no longer show visual checkmarks. Added the requested mobile headline wraps.

### Themes

- Corrected the hero phone composition so the app screen meets its frame and the overall graphic scale matches the supplied layout more closely.
- Built the later Figma Make direction into the second section: all ten named themes are selectable, Light/Dark mode is selectable, the preview typography/palette/caption update together, and state changes use 300ms transitions.
- Moved the full theme palette into PetAppro CSS variables instead of storing raw color values in the component.
- Applied the later entitlement decision: every plan can show the provider’s own logo; Crew and above remove the small co-branding mark.
- On mobile, moved the hero swatch fan up and right, exposed the full theme-picker tile contents, and wrapped “light and dark” onto its own line.
- Theme previews now render the Woof WeTreats logo white for every Dark selection while preserving the original logo treatment in Light mode.
- Mobile theme-picker cards are 12px narrower than the rail, with balanced inline padding and centered mandatory snap stops so card outlines and contents remain visible at every selection.

### Download, Support, Contact, and Sign Up

- Download uses the exported store/device assets and canonical launch copy. The Provider Side and Client Side controls are real ARIA tabs that replace the displayed screens, and all requested heading wraps are responsive. The email input retains an accessible name without a visible “Email address” label.
- Support contains all 29 Q&As from `support-faq.md`. Only the four top-level topics expand/collapse; individual questions are always expanded and are not interactive.
- Replaced the temporary text caret on each Support topic with Danny’s exported `icon-chevron-down.svg` and `icon-chevron-up.svg`. Native `<details>` state swaps the exact Figma assets, with a reduced-motion-safe opacity transition.
- Increased the tokenized spacing between the FAQ accordion and the “A full help center…” closing note.
- Contact uses the canonical support address and request checklist, with the later safety-language adjustment from the copy audit.
- Sign Up remains a pre-launch waitlist form and uses Title Case metadata.

### Policies

- Built the Figma policy-card index while preserving the established legal ownership model.
- No PetAppro policy body was created. Privacy, Terms, and all other policy cards resolve to `https://base509.com/policies/...`.

## Exploratory Provider Portal

The portal is under `/portal`, uses a distinct provider shell, and is labeled “Exploratory · Static Mockup” in the persistent header. It is intentionally `noindex` and contains no auth, database, booking, subscription, or payment wiring. Filters, editors, menus, local-file previews, and local downloads work in the browser so the proposed workflows can be evaluated without implying production data integration.

| Route | Concept |
|---|---|
| `/portal` | Clickable daily summary cards with client/pet detail, pending requests, and past-due names and amounts. |
| `/portal/clients` | Searchable roster with status-card filtering, a full Manage Client overlay, and a working local CSV export. |
| `/portal/business` | Auto-generated identity initials, logo upload, shared client-app theme picker, service-area editor, services/rates, booking rules, and team/role management. |
| `/portal/availability` | Woof-derived default holiday calendar, custom holiday pricing dates, blocked/limited availability, booking-demand indicators, and per-date service controls. |
| `/portal/billing` | Current plan, hosted Stripe Connect onboarding concept, corrected invoice-download actions, year-to-CSV export, and plan-comparison overlay. |
| `/portal/account` | Personal details, MFA, passkeys, active sessions, and provider-portal notification/security preferences. |
| `/portal/marketing` | Figma-based social graphic customizer with provider logo/name, live-service badges, provider code, preview, and working local PNG export. |
| `/portal/reports` | Period/comparison/service filters, KPI drill-downs, Revenue, Bookings & Demand, Customers, Rate Mix, and working CSV export. |

The portal shares PetAppro tokens, `.type-*` typography, button styles, icons, panels, status treatments, and responsive layout primitives. Active action buttons use Brand Accent with the darker Camo token for hover/press.

### Account Navigation

- Added the account holder avatar and first name to the portal header. Its popup links back to the PetAppro website, Account Settings, a future live-client-app view, and a mock Sign Out action.
- Added the same account affordance to the marketing-site header. While the product remains pre-launch, its popup contains only the email-notification form rather than implying that live sign-in is available.

### Social Graphic Reference

- Inspected Figma node `280:4185` and its logo/no-logo children `273:3814` and `278:3975`.
- Used Danny's exact `petappro_social_ad_template.png` asset at its 958 × 1200 source size.
- The generated PNG adapts between logo and name-only identity layouts and includes only the three MVP service badges: Boarding, Daycare, and Walking. Training, Drop-In Visits, In-Home Sitting, and Grooming are visible as disabled future options.
- The Provider Code is now read-only and labeled Auto-Generated. The concept explains that production assigns a unique public new-client code to the provider business at account creation and embeds the same code in its smart link and QR. It is explicitly separate from private/pre-approved client invitations.

### Client Management

- “Manage” opens a structured client workspace rather than navigating to an unfinished route. It includes Overview, Pets, and Booking History tabs, provider-only notes, contact and emergency details, pet profiles, and mock actions for editing, messaging, inviting, booking, and archiving.
- Status summary cards continue to filter the roster. Exported CSV rows now include email and phone details.

### Business, Roles, and Services

- Business initials update live from the business name: one word uses one initial; two or more words use the first initials of the first two words. A selected logo overrides the initials treatment.
- The Client App theme overlay reuses the same interactive picker as the public Themes page, including theme and Light/Dark controls.
- Replaced the earlier single-radius Service Area overlay with one shared Zone Manager. Business Profile and the Dog Walking service builder open the same tool; providers can create named zones, choose radius or custom-boundary concepts, and maintain one reusable source list for mobile service builders.
- Every active service has an optional Extra Dog Rate. In-Home, Training, and Grooming remain visibly locked as post-MVP tabs.
- Normalized the Boarding and Daycare Services & Pricing cards to shared internal rows. Headings, Base Price, Holiday Price, Extra Dog Rate, the reserved Boarding-only discount area, and footer notes align across both cards; wrapped text no longer shifts later fields.
- Corrected Extended pricing: it is now an “Extended Rate Discount” inside Boarding with a default value of $5 and a concise “stays of 8+ days” explanation. The incorrect shared “Extended Care” surcharge was removed.
- Booking Rules uses a square-cornered text editor. Team roles now follow the canonical permissions document: Owner, Admin, Staff, and Client. Owner and Admin are distinct; the Owner retains ownership, billing, transfer, and deletion authority.
- Permission controls use the exact checked-state asset and proportions from Figma node `247:12`. Selects use the exact chevron treatment from node `248:3`, with adequate right padding. Newly added staff can be removed immediately.

### Dog Walking and Scalable Service Pricing

- Reworked Services & Pricing around compact semantic tabs rather than tall pill buttons. The current order is Boarding & Daycare, Dog Walking, In-Home, Training, and Grooming. In-Home, Training, and Grooming remain visibly faded and disabled as future service families; the In-Home tab is accessibly described as covering Drop-In Visits and In-Home Sitting.
- Built Dog Walking as a first-class MVP editor using the canonical duration-tiered model. Providers can offer Individual, Group, or Both; configure service location, dogs per walker, travel buffer, daily walker limit, Meet & Greet, recurring walks, business service area, and the proposed Crew+ GPS setting.
- Added independently editable 30- and 60-minute example rates for Individual and Group walks. Providers can add, duplicate, hide, remove, or convert rate options, with Group rates priced per dog and capacity kept separate from pricing. Group rate creation defaults to the selected Group context.
- Kept the UI scalable rather than encoding 30/60-minute behavior as special cases: every row is a generic duration/type/rate/capacity option. Production persistence and calculation must still route through the shared pricing engine and tenant-scoped server boundary.
- Modeled Puppy Rate as an optional Puppy Handling Surcharge with one amount and independent Boarding, Daycare, and Dog Walking applicability checkboxes. This is distinct from the Boarding-only Extended Rate Discount.
- Corrected the shared portal switch primitive discovered during visual QA. Selected toggles use the dark Brand Accent state; deselected toggles use the neutral inactive-control token, and the track uses only defined spacing tokens so it no longer collapses.
- An earlier exploratory Holiday Surcharge card was removed after review against locked decision D-039. Holiday pricing now uses explicit service-level rate overrides rather than a global flat or percentage adjustment.
- Added semantic inactive-control tokens derived from Maverick Grey. Off-state toggle tracks are now visibly darker without resembling the active green state, and unchecked checkbox outlines have stronger neutral contrast throughout the portal.

### Walk Windows and Client Group-Walk Booking

- Added Duration-Tiered Pricing and Availability Walk Windows as a two-tab configuration set inside Dog Walking. Add Rate Option remains intact as the scalable duration/type/price editor; no rate data is nested in or duplicated by the scheduling windows.
- Added editable window names, day chips, walk-start time bands, Individual/Group service controls, selectable shared-zone chips, active/hidden states, and Duplicate/Remove actions. Add Window creates a hidden draft.
- Lifted Dogs Per Walker and Max Walks Per Day into the parent Business state. Window capacity is computed from the same Dogs Per Walker value used by Walking Rate Options rather than copied into another input.
- Removed the provider-visible Solo / Duo & Up preview control. Plan behavior is now implicit from the account subscription: Solo omits assignment UI and multiplication; Duo/Crew renders staff assignment and computed capacity without asking the provider to identify their plan.
- Added per-window zone selection and per-walker Coverage Zones for team plans. A provider can combine days, time band, zones, services, and staff assignments in each window; Solo providers can still vary zone coverage between Morning, Midday, and Evening windows.
- Added the Group Walk Policy card above the window list with the optional Min Walks / Week field and the approved recurring-schedule, one-off-extra, approve-anyway, and provider-created-booking guidance.
- Added a responsive static client-app phone concept for Group Walk booking: day selection, live-spots window cards, disabled Individual-only windows, Weekly/Just This Week recurrence, “Request a Spot” CTA, Booking Rules note, en-route/live-GPS trust note, and after-walk report-card copy with actual start/end times.
- Explicitly excluded the group-builder route view, frequency pricing, and schema/backend work. Danny’s later direction supersedes the rev-3 inherited-single-area MVP decision for this exploratory UI by showing reusable zones and per-window coverage; Product Management still needs to ratify the schema/granularity change before production implementation.
- Standardized every PetAppro native select on Danny’s exported 24px Chevron Down asset and removed the older select-caret asset from runtime source. Service Area and Reports previous/next controls now use the same Chevron family.

### Availability and Holiday Pricing

- Added `/portal/availability` and a persistent Availability navigation item. The page keeps holiday pricing, availability closures, service-level limits, and booking demand visually distinct on one responsive calendar.
- Added an explicit multi-day editing mode. Providers can toggle any combination of dates, select the entire visible month, return to a single date, and apply holiday pricing, Open/Limited/Blocked availability, service closures, and a shared staff note across the selection. Mixed holiday and availability states are explained before a bulk value is applied.
- Reduced calendar date tiles and increased both row and column spacing so selected outlines remain visually separated. The responsive grid retains a square tile treatment and fits without horizontal overflow at 390px.
- Reproduced the Woof WeTreats default holiday rules: New Year’s Day/Eve, Presidents Day weekend, Memorial Day weekend, Independence Day weekday/weekend behavior, Labor Day weekend, Veterans Day weekday/weekend behavior, Thanksgiving Thursday–Sunday, and December 24–25.
- Providers can add or remove individual holiday dates, remove a default period, add custom dates, and reset the displayed year to Woof defaults. Custom dates appear separately in the holiday list.
- Each date can independently use holiday pricing and remain Open, Limit Services, or Block All. Limited dates expose Boarding, Daycare, and Dog Walking checkboxes plus an optional staff-only note. Calendar badges demonstrate existing booking counts without making occupancy an availability rule.
- Changed booking-count badges and the Bookings legend marker to Brand Accent green so they remain clearly distinct from Brandy Blue holiday-pricing indicators.
- Replaced the calendar’s compact select-chevron asset with Danny’s exported 24px PetAppro Chevron asset, rotated for previous/next. Removed the remaining text-based caret characters from the account menu, shared card carousel, and theme phone preview so navigation arrows consistently use icon assets.
- Replaced the global Holiday Surcharge amount with explicit Holiday Price fields inside Boarding and Daycare. Every Individual/Group walking duration now has its own regular and holiday rate, so a short walk does not inherit the same adjustment as a multi-night stay.
- This is still a local static concept. Production requires tenant-scoped holiday and availability records, RLS tests, authorized server mutations, immutable booking-price snapshots, and reuse of the shared pricing/availability engines.

### Payments, Plans, and Security

- Stripe Connect is represented as an Owner-only hosted onboarding flow: PetAppro performs a security check, sends the provider to Stripe, and stores connection/status identifiers after return. Providers do not paste Stripe API or secret keys into PetAppro.
- Rebuilt Payments to `docs/specs/payments-stripe-connect.md`: the page explicitly separates Provider → Base509 subscription billing from Client → Provider booking payments, identifies Standard connected accounts/direct charges/$0 application fee, and implements the Before / Connect / After flow with Not Connected, Pending, Enabled, and Action Needed prototype states. All Stripe calls and return states remain local stubs.
- Added the revised provider-choice payment model: Cash and the grouped Venmo / Zelle / CashApp option are offline collection methods with manual paid tracking; the grouped payment-app option includes an optional handle/instructions field. Card is the only in-app-processed method.
- Reorganized Billing into two explicit full-width workflows. “Manage Your Subscription and Business Payments” introduces the provider’s PetAppro plan, while “Set Up How Clients Pay Your Business” now appears after subscription management and immediately before accepted payment methods/Stripe setup. Removed the ambiguous “Two Separate Money Streams” heading and the prior two-column explainer layout.
- The Card checkbox now gates the entire Stripe Connect module. Unchecked leaves the progress tracker and controls visibly faded and semantically disabled; checked activates them. Its status chip maps Connect state to Not Set Up, Pending, or Active.
- Moved the Connect progress tracker to the top of the module, directly below its header. Added a client-facing checkout preview that updates to show the currently accepted methods and optional payment-app instructions.
- Invoice downloads use the exact Figma download glyph from node `257:1041`. Compare Plans opens the shared public plan matrix with prices.
- Account Settings now surfaces MFA, passkey enrollment, recovery guidance, and active sessions. Server-verified MFA/re-auth is the proposed authority for sensitive money and PII actions; an optional native biometric lock would complement it, not replace it.
- The authenticator overlay now shows an account-specific QR enrollment preview and explains that production generates a unique TOTP secret/provisioning QR when setup begins. The prototype QR is deliberately non-functional; production secrets and provisioning URIs must never be logged.
- Added a guarded Delete Account flow with policy links, export/retention/Stripe/business-ownership warnings, acknowledgment, and a typed `DELETE` confirmation. It distinguishes individual-account deletion from subscription cancellation and business closure; no destructive action is wired in the static portal.
- Made the closure entry-point language explicit before the user opens the modal: “See Terms of Use” links directly to the canonical Terms, followed by direct Privacy Policy and Account Ownership Policy links explaining deletion/retention and sole-Owner transfer or business-closure requirements. The same explicit links are repeated in the confirmation overlay.
- The public “Features by PetAppro plan” matrix (on Pricing) now has padded caption space and inverse/white column-header text on its dark header.

### Business Reports

- Added a responsive Reports route based on Danny’s screenshots and `docs/specs/provider-reports.md`.
- Week, Month, Quarter, and Year periods, previous/next date navigation, a calendar input, Previous/YoY comparison, and service filtering are interactive.
- KPI cards cover Earnings, Collected, Bookings, New Clients, Average Booking, and Referrals / Discounts; each opens a detail overlay.
- Revenue, Bookings & Demand, Customers, and Rate Mix views include responsive line, bar, and donut visualizations plus the requested customer cadence and top-client information. CSV export creates a local file from the selected mock view.
- All report data is deterministic sample data; no production booking, pricing, payment, or client data is wired.

### Business Page Density Pass (2026-08-15, Cowork covering session for Codex)

- Split Business Settings into four in-page sub-tabs along the page’s existing seams — Profile & Brand, Services & Pricing, Booking Rules, and Team — reusing the underline tab pattern the Service-families row already uses. Chose in-page tabs over new routes so the split needs no navigation or URL changes while the Walk Windows IA question is still open with Danny; converting a tab to a route later is mechanical.
- Replaced the single page-level Save Changes with a scoped Save at the end of each sub-tab (Save Profile & Brand, Save Services & Pricing, Save Booking Rules, Save Team). Each reports a tab-scoped notice in the existing static-concept phrasing.
- Collapsed walking rate cards to one-line summary rows at rest (for example “Group Walk · 30 min · $22/dog · +$8 extra dog” with the existing Bookable/Hidden status pill). Clicking a row expands the full editable card; a chevron control collapses it; one card expands at a time and there is exactly one level of collapse, per the approved spec.
- Collapsed Walk Windows to the same summary treatment (“Midday · M–F · 10–2 · 12 dogs · 2 walkers · Bookable”). Consecutive day runs compress to ranges (M–F) and nonconsecutive days list out (M · W · F); time bands compress to short start–end forms (10–2, 4–6:30); group-less windows read “Individual only”; walker counts render only on team plans, preserving the Solo no-staff-machinery rule. Add Window now opens the new draft expanded for editing.
- The first cut kept a Bookable/Hidden status pill in the collapsed rows; Danny reviewed live and directed that the active toggle IS the status — see the same-day feedback section below where the pill was removed.
- Moved the client phone preview out of inline scroll content and behind a Preview Client View button that opens a right-side drawer. Added a shared PortalDrawer component in PortalShell reusing the modal backdrop and header patterns; the full booking concept — day chips, live-spots window cards, recurrence, approval guidance, trust notes, and the Request a Spot CTA — works unchanged inside the drawer.
- Demoted the two teal explainer panels (“How this works” on Walk Windows; “Pricing and capacity stay separate” on Duration-Tiered Pricing) behind a shared PortalHint toggle: a text button that expands the existing note styling on demand, collapsed by default because both sections start with data. Explainer copy is unchanged.
- Removed the now-unused portal-client-walk-concept styles after the preview moved into the drawer; added shared portal-drawer, portal-business-tab-panel, portal-tab-save, portal-collapse-summary / -controls / -toggle, and portal-hint classes to brand-petappro.css using existing tokens only — no raw values.
- Did NOT relocate Walk Windows out of Business. The Availability-nav move requires Danny’s sign-off; the written IA reconciliation proposal is recorded under Open Questions.
- Operational note: running `npm run build` while the port-3003 dev server was serving overwrote its `.next` directory and the portal returned 500; restarted the same `npm run dev -- -p 3003` server to restore it. Future sessions should expect to restart the dev server after any production build.
- FILES TOUCHED (this session): `apps/web/src/components/PortalShell.tsx` · `apps/web/src/components/PortalBusinessView.tsx` · `apps/web/src/components/PortalWalkingRates.tsx` · `apps/web/src/components/PortalWalkWindows.tsx` · `apps/web/src/styles/brand-petappro.css` · `docs/BUILD-LOG-overnight.md`

### Density Pass Feedback Round (2026-08-15, Danny live review during the same session)

- Danny reviewed the first density cut live and sent five corrections; all are built and verified below.
- Collapsed-row anatomy: the Bookable/Hidden pill was removed from summary rows because the active toggle communicates the same state (“the ‘Bookable’ should be the toggle in those rows”). Rows are now title · summary details · toggle · chevron. The redundant pill was also removed from expanded-card footers, which now hold only Duplicate / Remove.
- Static header geometry: collapsed and expanded states share one identical header row — the title, toggle, and chevron do not move at all between states; only the chevron glyph flips (Down/Up) and the editor body appears beneath. Verified by measuring identical getBoundingClientRect positions for title, toggle, and chevron before and after expanding. Renaming a window happens in a Window Name field at the top of the open body, per Danny, so the row title stays put.
- Zone management moved out of the configuration tabs: the Walk Windows header’s Manage Zones button and each window’s Edit Zone Map link were removed, replaced by a Service Zones control in the Dog Walking general settings (above the Pricing / Walk Windows tabs) that opens the shared Zone Manager. Framing follows Danny’s direction that zones are managed per service — each mobile service (In-Home etc.) will manage its own zone set later. Per-window zone selection chips remain inside each window.
- Progressive disclosure by walk offering: choosing only Individual now hides Walk Type selects, the Dogs Per Walker setting, capacity fields, the Group Walk Policy card, per-window Services checkboxes, and group capacity lines (window summaries read “Individual only”); choosing only Group mirrors this (all rates read Group Walk, group machinery stays); Both surfaces everything. Effective per-window service flags derive from the offering so stale checkbox state cannot leak through.
- Service Active off now fades and disables the entire Dog Walking body below the service description and toggle (opacity treatment plus the `inert` attribute so nothing is focusable). Boarding and Daycare got the equivalent treatment: switching a service card off fades its fields and disables its inputs while the heading and toggle stay interactive.
- Removed every customer-visible “MVP” mention per Danny: the Dog Walking eyebrow is now “Core Service”; the Team note reads “Custom permission sets arrive after launch.”; the Walk Windows concept note reads “…arrive after launch.” (also dropped its internal schema sentence); the public Pricing footnote reads “in-app messaging arrives after launch.” These are copy edits Danny requested live; the Pricing footnote change touches marketing copy sourced from the copy docs, so the copy docs should be reconciled to match.
- FILES TOUCHED (feedback round): `apps/web/src/components/PortalBusinessView.tsx` · `apps/web/src/components/PortalWalkingRates.tsx` · `apps/web/src/components/PortalWalkWindows.tsx` · `apps/web/src/styles/brand-petappro.css` · `apps/web/src/app/petappro/pricing/page.tsx` · `docs/BUILD-LOG-overnight.md`

### Services & Pricing Cleanup Rebuild — Dog Walking (2026-08-15, Cowork covering session, second pass)

- Rebuilt the Dog Walking tab to the approved `design_working/services-cleanup-mock.html`: the Duration-Tiered Pricing / Availability Walk Windows tab pair is gone, replaced by three always-visible numbered sub-cards — ① Service Basics (walk-type selector, Service Location, Service Zones, Dogs Per Walker, Travel Buffer, Max Walks Per Day, feature chips; Service Active in the sub-card header), ② Pricing — Walking Rate Options, ③ Availability — Walk Windows. New shared `portal-subcard` / `portal-stepnum` classes; the mock’s teal step circles map to Brand Accent tokens because the mock is not authoritative for primitives or colors.
- Built the connector strip between ① and ② with the mock’s copy (② Rates — what a walk costs × ③ Windows — when walks happen = What clients can book) and a worked example that follows the walk-type selection: Group/Both shows the group example, Individual-only shows “30-min Individual · $30” → “Individual 30-min · Midday M/W/F · $30”.
- Preference checkboxes became `portal-chip` toggle chips per the mock; the GPS chip keeps its Crew+ badge and drops the “Enable” prefix per mock copy. Deviation flagged below: Max Walks Per Day stayed an editable input in the Basics grid rather than the mock’s dashed chip — a chip cannot edit a number, and the primitives rule keeps existing controls.
- Collapsed rows now match the approved formats — rates “Individual · 30 min · $30 · +$10 extra dog · 1 household” and windows “Midday · M–F · 10–2 · Individual + Group · 12 dogs · 2 walkers” — with status pills visible at rest, the single `icon-chevron-down.svg` asset rotating 180° when open, and rows as keyboard buttons announcing `aria-expanded`. The existing rate and window editors render unchanged on expand; window renaming stays in the open body; the Bookable enable switch plus Duplicate / Remove live only inside the expanded state. Midday starts collapsed like its siblings.
- Add Rate Option now creates a hidden draft rate expanded in place, mirroring Add Window; the separate Add Walking Rate form and its draft state were removed.
- Group Walk Policy is the first, tinted, collapsed row inside ③ with a live summary (“Min 3 walks/week to start a recurring schedule · one-offs & approvals still allowed”); expanding reveals the existing policy editor. When no minimum is set the row reads “No weekly minimum · one-offs & approvals still allowed” — a fallback the mock doesn’t show, flagged below.
- “How this works” prose moved behind ⓘ icons in the ② and ③ headers (shared PortalInfo replaces PortalHint); the verbatim note reveals at the top of the sub-card body using the same inline-reveal overlay pattern as before, now with an icon trigger.
- Spec §2A conditional rendering: effective window service flags are the intersection of stored config × the offering selector. Individual-only hides Group rates, the policy row, Dogs Per Walker, and window Services checkboxes, switches window capacity copy to walks (“up to 12 walks”), and shows the quiet notice “2 Group rates and 1 policy hidden — restored if you re-enable Group.” Group-only hides Individual rates with the mirrored notice and flags stored Individual-only windows with an amber “Not Offered” pill; a window offering neither type under the current selection gets the same flag. Switching filters only — nothing is deleted.
- Client preview gains a “1 · Individual or Group?” step only when Both is offered (later steps renumber); the phone title, CTA price ($22/walk group, $30/walk individual), group-only spots pills, and disabled-window captions follow the selection. “Group walks only” was added as the individual-side analog of the existing “Individual walks only” caption — new microcopy, flagged below.
- Service Surcharges collapsed to a plain sub-card below the family tab panels: grey ＋ badge, live summary (“Puppy Handling · $10 · Boarding & Daycare”, tracking amount and applied services), the enable toggle, and a chevron revealing the existing controls.
- ③’s closing note is now the mock’s “Rates and windows stay independent — windows never set prices, rates never set days.”, superseding the previous concept note (including its internal schema sentence).
- CSS: added the portal-subcard family, portal-stepnum(+muted on Maverick Grey), portal-eq strip (result box on Brandy 800 with inverse text), portal-info-toggle, portal-walk-policy-row, and portal-restore-note; removed the dead config-tab, walking-overview, walk-preferences, add-rate-form, walk-rates-header, hint, collapse-controls, and pricing-rules-wrapper rules. Chevron rotation is attribute-driven via `[aria-expanded='true']`.
- Task-prompt truncation note: item 5 arrived cut off (“Zone Manager: the Service Zones field in ① opens the…”); implemented as opening the existing shared Zone Manager modal, which that field already did. Say if more was intended.
- FILES TOUCHED (this session): `apps/web/src/components/PortalShell.tsx` · `apps/web/src/components/PortalWalkingRates.tsx` · `apps/web/src/components/PortalWalkWindows.tsx` · `apps/web/src/components/PortalBusinessView.tsx` · `apps/web/src/styles/brand-petappro.css` · `docs/BUILD-LOG-overnight.md`

### Portal Commit-Prep Pass (2026-08-15, Cowork covering session, third pass — Danny’s page-by-page edits + live feedback)

- Danny authorized committing the portal concepts and sent page-by-page edits; all are built, verified, and included in the commit recorded below.
- **Dashboard:** the fourth stat card still reads Active Clients 42, but the list it opens is now titled “Newest Clients” with the four most recently joined and the caption “4 most recently joined of 42 active — see Clients for the full roster,” resolving the 42-vs-4 confusion by specifying the list (Danny’s newest-joined option).
- **Clients:** the roster now actually contains 42 active clients (plus 4 pending invites and 3 meet-and-greets, matching all three stat cards), generated deterministically and sorted alphabetically. The table sits in a scroll box showing exactly 10 rows with a sticky header (scroll chosen over A–Z tabs — simpler and Danny offered either). The “sample records” caption now reads “{n} clients.”
- **Reports:** line charts now render fluid monotone-cubic curves (the same monotone interpolation Woof WeTreats’ Recharts charts use) instead of straight polylines, with a normalized draw-in animation (`pathLength`-based) and a staggered grow on the bookings bars; dashed grids, rounded bar tops, and the donut scale-in were already in place, and colors/fonts are untouched per Danny’s constraint. Reduced-motion users still get static charts.
- **Business / Zone Manager:** the map is now a real Google Maps integration (`@googlemaps/js-api-loader` + `terra-draw` + `terra-draw-google-maps-adapter`, the approved Google Maps + Terra Draw direction). Zones render as live circles (radius mode, centered on the business anchor or the zone’s own center) and polygons (custom boundaries, sample GeoJSON rings for Mission and Castro); Draw Boundary starts a Terra Draw polygon and the finished ring saves onto the zone. The map is gated on `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` in `apps/web/.env.local` (gitignored) and falls back to the previous illustrated map with a key-required note when unset. ⚠️ Verification limit: no key exists on this machine, so only the fallback path was exercised in the browser; the live map/drawing path compiles and follows the adapters’ documented APIs but needs a first run once Danny adds a domain-restricted browser key.
- **Booking Rules:** replaced the placeholder rules with Danny’s 11-item boilerplate verbatim (Meet & Greet first → Terms of Service), including the bracketed emergency-vet placeholder (one typo inside the placeholder fixed: “name of preferred”). The textarea grew to fit.
- **Marketing:** Provider Code was already read-only with the “Assigned Automatically” callout — confirmed unchanged. The real QR code for the graphic is logged below as a pre-launch TODO (target URL doesn’t exist yet).
- **Billing:** the Current Plan card is now clickable (opens Compare Plans; detail reads “Click to change plan”), and the PetAppro Subscription panel gained a “Change Plan” action so plan changes are findable — this answers Danny’s “I cannot find this” by making the Compare Plans matrix the change-plan destination in the mock. Full Stripe test-mode verification is logged below as a pre-launch TODO.
- **Account:** the TOTP jargon is gone — “Six-digit codes from your authenticator app protect…” — and the MFA modal copy was rewritten in user language (unique enrollment QR, recovery codes) without acronyms or production-vs-concept framing.
- **Build-note removal (site + portal):** deleted the “Exploratory · Static Mockup” badge, the “Concept screens only” sidebar note, every per-page “Exploratory interface…” concept paragraph, all “…in this static concept.” notice suffixes, the “Concept Screen Only” pill, the “· Stub” button label, “Prototype Status Preview” (now “Preview Status”), and the concept map caption. The marketing pages’ “Illustrative screens — sample content” footnotes were kept deliberately: they are product disclaimers about screenshots varying by provider/plan/theme, not build notes. The account-deletion demo notice was removed rather than reworded (silent close instead of a false claim).
- **Live feedback during the pass:** (1) equation-strip boxes were pill-shaped via the 999px control-radius token and the result example rendered dark-on-dark — both fixed (8px corners from the spacing scale, explicit inverse text). (2) The Bookable toggle moved back into each collapsed row (replacing the status pill position) and left the expanded footer — one control, no redundancy; the windows list keeps only the amber “Not Offered” flag pill. (3) Step circles grew 50% (24px → 36px, body-size digits) so the 1-2-3 sequence reads clearly.
- **Per-walker, per-window capacity (Danny’s direction, built):** each walker now has a default “Dogs Per Walk” in Service Basics (Morgan Lee 6, Casey Reed 4 sample) and an editable “Dogs This Window” override next to their name and Coverage Zones inside every window — exactly the default-at-top-plus-per-window-override model Danny settled on. Window group capacity is the sum of assigned walkers’ effective caps (Midday = Morgan 6 + Casey 4 = 10), and a “Zone capacity from walker coverage” line shows each zone’s max from the walkers covering it (Noe Valley 4 when only Casey covers it — Danny’s worked example). Solo plans keep the single Dogs Per Walk field; group rate cards no longer carry a per-rate Dogs/Walker on team plans.
- FILES TOUCHED (this session): `apps/web/src/components/PortalShell.tsx` · `PortalDashboardView.tsx` · `PortalClientsView.tsx` · `PortalReportsView.tsx` · `PortalBusinessView.tsx` · `PortalWalkingRates.tsx` · `PortalWalkWindows.tsx` · `PortalZoneManager.tsx` · `PortalZoneMap.tsx` (new) · `PortalMarketingView.tsx` · `PortalBillingView.tsx` · `PortalAccountView.tsx` · `apps/web/src/styles/brand-petappro.css` · `apps/web/package.json` · `apps/web/package-lock.json` · `apps/web/.gitignore` · `docs/BUILD-LOG-overnight.md`

### Portal Functionality Round (2026-08-15, Cowork covering session, fourth pass — Danny's post-commit edits)

- Removed the native number-spinner arrows from the Dogs This Window field — only the plus/minus icon buttons step the value now, no duplicate controls.
- Marketing: the logo placeholder initials now derive live from the business name (first letter of the first two words, up to two characters) — typing "Marco Polo Care" shows "MP".
- Billing plan change is now a real flow: "Change Plan" is a proper button (the hard-to-see text link is gone), and Compare & Change Plans carries selection radios pulled from the PetAppro app design (Figma node 247:16 — 20px circle, 1.5px inactive border, checked = 2px Brandy 600 teal ring with a 10px teal dot, mapped to the existing --pa-brandy-600 / --pa-control-inactive-border tokens). The current plan is checked and tinted; selecting another column outlines it in Brand Accent green; a confirmation block states the switch, that it takes effect immediately, and the prorated amount for the days left in the cycle (charge on upgrade, credit on downgrade); confirming updates the plan portal-wide.
- Plan entitlement gating: new shared PortalPlanProvider (React context in the portal layout) derives entitlements from the canonical pricing MATRIX — in-app card payments (Duo+), GPS walk tracking (Crew+), seat limits per tier, and the Solo/Duo/Crew walker UI tier. On Starter/Solo the Card (Stripe) payment option, the entire Stripe Connect panel, and the checkout Card method are hidden (not disabled — hidden, per Danny's "should not see or interact" rule); the GPS chip disappears below Crew; Team seat limits and copy follow the tier. Downgrading to Solo in Billing cascades live to Business (walker machinery gone, single Dogs Per Walk field). Theme-library tier gating in the theme picker is logged below as a follow-up.
- Discount codes: a Discount Code panel on Billing applies sample codes (WELCOME20 20%, MAKEGOOD10 10% courtesy, TESTER-FREE 100% tester access), reprices the Monthly Price and Next Renewal stats, shows the applied code with Remove, and rejects unknown codes. Production codes are issued from the future Base509 admin dashboard (roadmap note below).
- Account: Personal Details is now editable (Name, Email, Time Zone; Role locked as server-assigned), and the MFA modal lists compatible authenticator apps ("Google Authenticator, 1Password, Microsoft Authenticator, or Authy").
- Clients: Add Client now works — a modal collects name, email, optional phone and pet, validates name+email, adds the record as an Invite Pending row (roster resorts alphabetically), switches to the Pending filter, and bumps the live stat counts (cards now compute from the roster instead of hardcoded numbers).
- Measurement note: Billing at 390 reports documentElement.scrollWidth 562 while body.scrollWidth is 390 and the page cannot actually be scrolled horizontally (verified by scroll attempt; no unclipped element extends past the viewport) — the raw figure is inflated by the internally-scrollable invoice table, so the responsive check for Billing is "cannot scroll horizontally" rather than the strict scrollWidth equality.
- FILES TOUCHED (this session): `apps/web/src/components/PortalPlanProvider.tsx` (new) · `PlanComparisonTable.tsx` · `PortalBillingView.tsx` · `PortalBusinessView.tsx` · `PortalWalkingRates.tsx` · `PortalClientsView.tsx` · `PortalAccountView.tsx` · `PortalMarketingView.tsx` · `apps/web/src/app/portal/layout.tsx` · `apps/web/src/styles/brand-petappro.css` · `docs/BUILD-LOG-overnight.md`

### Discount-Code Error States (2026-08-15, Cowork covering session, fifth pass)

- Bad discount codes now fail with specific inline errors at the field (danger-token text and input border, `role="alert"`, cleared as soon as the user types): unknown code → "isn't a valid discount code. Check the spelling and try again."; expired → "expired on July 31, 2026 and can no longer be applied."; not yet in effect → "isn't active yet — it starts October 1, 2026."; one-time already redeemed → "is a one-time code that has already been used." Sample codes were added for each state (SUMMER15 expired, LAUNCH50 not-started, THANKYOU25 redeemed) alongside the three working ones; dates use the long form per house style. All four states plus error-clearing and the happy path verified in-browser; `npm run build` PASS (54 pages).
- FILES TOUCHED (this session): `apps/web/src/components/PortalBillingView.tsx` · `apps/web/src/styles/brand-petappro.css` · `docs/BUILD-LOG-overnight.md`

### Site & Theme-Gating Round (2026-08-15, Cowork covering session, sixth pass)

- The public site no longer presents a logged-in account: the marketing header trigger now reads "Sign In" (no avatar or name) and opens the existing pre-launch popover — "We'll let you know when sign-in is ready." with the notify-me form. The portal keeps its signed-in menu; only the marketing variant changed, matching Danny's "you can't actually log in until we're ready to accept subscribers."
- Pricing page feature matrix now shows each plan's price in the column header (same treatment as the portal's compare view, no selection radios) with "Most Popular" badges on Duo and Crew — badge below the price per Danny's live note — and a light tint down both popular columns. Danny also asked for top-aligned column headers mid-pass; `.matrix thead th { vertical-align: top }` applied.
- Theme gating: ThemeGallery accepts an `allowedThemes` filter and the plan context now carries a theme allowlist — Solo (and Starter, see below) sees exactly Brandy Blue, Husky, and Irish Setter in the portal's Choose Theme picker; Duo and up see all ten. Locked themes are hidden, not disabled, per the established tier rule. Verified live: Crew shows 10 swatches, downgrading to Solo shows 3. The public Themes page is unaffected.
- ⚠️ Two reconciliation flags for Danny/PM: (1) Starter — Danny said "solo plan user"; Starter was given the same three-theme allowlist rather than more than Solo gets, but the locked pricing matrix says Starter = Brandy Blue only. (2) The locked matrix also says Duo gets breed themes but NOT the full city/seasonal library (that's Crew+), while Danny's direction gives all other plans everything. RESOLVED same day: Danny ruled the locked matrix stands; the portal now derives the allowlist from the MATRIX rows (see Theme-Tier Reconciliation entry).
- Checks: `npm run build` PASS (54 pages); pricing header shows prices with Duo/Crew badges after the price, 32 tinted popular cells, zero selection radios on the public page, computed `vertical-align: top`; Sign In popover shows the pre-launch form with no signed-in identity; theme counts verified on Crew (10) and Solo (3) via the live plan-change flow.
- FILES TOUCHED (this session): `apps/web/src/components/AccountMenu.tsx` · `PlanComparisonTable.tsx` · `ThemeGallery.tsx` · `PortalPlanProvider.tsx` · `PortalBusinessView.tsx` · `apps/web/src/app/petappro/pricing/page.tsx` · `apps/web/src/styles/brand-petappro.css` · `docs/BUILD-LOG-overnight.md`

### Theme-Tier Reconciliation (2026-08-15, Cowork covering session — Danny ruling)

- Danny ruled on the sixth-pass flags: Starter is Brandy Blue ONLY, and the locked pricing matrix stands — the portal conforms; no doc, marketing-table, or theme-tiers.md changes.
- The portal's theme allowlist is now DERIVED from the canonical MATRIX rows (Default theme / Breed themes / All breed themes / Full library) instead of a hand-written tier check, so it cannot drift from the locked data again: Starter → Brandy Blue; Solo → + Husky, Irish Setter; Duo → + Bichon Frise, Blue Heeler, Chessie; Crew and up → full library including city + seasonal.
- Verified in-browser via the live plan-change flow: Starter shows exactly [Brandy Blue]; Duo shows exactly the six breed-tier themes; Solo (3) and Crew (10) confirmed in the prior pass with the same code path. `npm run build` PASS (54 pages).
- FILES TOUCHED (this session): `apps/web/src/components/PortalPlanProvider.tsx` · `docs/BUILD-LOG-overnight.md`

### Live Google Maps Wiring (2026-08-15, Cowork covering session — key delivered)

- Danny supplied the referrer-restricted browser key in `apps/web/.env.local` (gitignored; add the same var on Vercel later). The zone map now implements the full approved stack: Google Maps JavaScript API via the functional loader (no deprecated Drawing library), Terra Draw polygon drawing with GeoJSON output, Places API (New) autocomplete with proper session tokens (debounced, token discarded on selection) for the business-address search overlaid on the map, Geocoding API + geometry library for a "Check a client address" coverage tester (containsLocation for drawn polygons, spherical distance for radius zones), and anchor recentering (choosing an address moves the marker and recenters radius zones).
- GeoJSON-only guardrail honored: new `src/lib/service-area.ts` builds a FeatureCollection for the whole service area — custom zones keep their drawn rings; radius zones are converted to 64-segment spherical polygon rings computed dependency-free (no Google shape ever persisted, so the base map stays swappable). `saveServiceArea()` is the persistence stub, documented for the later Supabase/PostGIS drop-in (`geography(Polygon,4326)` via `ST_GeomFromGeoJSON`, business_id-scoped RLS); the Zone Manager's Done button captures and logs the collection.
- ⚠️ Live verification blocked at Google's gate: the map script loads and initializes, then Google rejects with `RefererNotAllowedMapError — Your site URL to be authorized: http://localhost:3003/portal/business`. The key's allowlist has `localhost:3000/*` but the dev server runs on 3003. Danny needs to add `http://localhost:3003/*` to the key's website restrictions; everything downstream (tiles, drawing, autocomplete, geocoding) is untested until then. Added a `gm_authFailure` handler so auth failures now degrade to the illustrated fallback map with the key-required note instead of Google's broken-map overlay.
- Note: Danny's task spec framed the MVP behavior as one Service Area (walk-windows §6.1); the exploratory UI keeps the multi-zone manager that today's per-window/per-walker zone capacities depend on, and the save stub reconciles the two by persisting the zones as a single service-area FeatureCollection. The §6 single-vs-multi zone question remains the open PM item already logged.
- Checks: `npm run build` PASS (54 pages); live map mounts (fallback correctly suppressed when the key is present); console confirms the exact referrer error; `.env.local` confirmed gitignored and absent from staging.
- FILES TOUCHED (this session): `apps/web/src/lib/service-area.ts` (new) · `apps/web/src/components/PortalZoneMap.tsx` · `PortalZoneManager.tsx` · `apps/web/src/styles/brand-petappro.css` · `docs/BUILD-LOG-overnight.md`

## Open Questions for Danny

- **Base509 admin dashboard (Danny, 2026-08-15).** Managing discount codes (promos, courtesy credits, free tester access) needs an internal Base509 dashboard — Danny explicitly wants his own management surface for PetAppro. Logged as a roadmap item for PM; the portal side ships with sample codes only.
- **Tier gating follow-ups.** Theme-library gating in the Business theme picker (full library is Crew+; breed themes Solo+) is not yet enforced by the new entitlement context — ThemeGallery is shared with the public site and needs its own pass. Same for Starter's 5-client cap on the Clients page.
- **Zone-demand visibility (Danny’s question, 2026-08-15).** Per the ratified capacity model, requests over a zone/window limit are never silently lost: auto-book only approves within capacity, so over-limit requests land in Pending Requests where the approve action warns and offers “Approve anyway (over capacity)” — the provider sees every one. What does NOT exist yet is an aggregated demand signal (“Noe Valley keeps maxing out”). Proposal for PM: add zone-level demand to Reports → Bookings & Demand (requests vs. capacity per zone/window, including declined/over-cap counts) and a dashboard attention item when a zone repeatedly hits its cap. Needs a decision before build.
- **Per-walker / per-window capacity needs spec ratification (2026-08-15).** Danny’s direction (default Dogs Per Walk per walker + per-window override next to name/zones, zone capacity = Σ caps of covering walkers) supersedes walk-windows §2’s uniform `Dogs Per Walker × walkers` formula and refines the capacity-model Walking row (“per-walker cap… scales by walkers”). The mockup implements the new model; the spec pass (and eventual `capacity_config`) should ratify it.
- **Google Maps key + first live-map run.** The zone map needs `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` in `apps/web/.env.local` (domain-restricted browser key per the approved Google Maps + Terra Draw decision). Until then the fallback illustrated map renders; the live draw path needs a smoke test once the key exists.
- **Pre-launch TODOs from Danny’s notes:** replace the Marketing graphic’s placeholder with a real QR code (target app URL once it exists), and run the provider Stripe Connect flow end-to-end in Stripe test mode before launch.
- **Commit scope note:** the commit below includes `apps/web`, this build log, and the two portal design mocks in `design_working/`. The rest of the dirty worktree (planning docs, specs, handoffs, other agents’ files) was deliberately left uncommitted for its owners.
- **Cleanup rebuild details for sign-off (2026-08-15, second pass).** (1) Max Walks Per Day renders as an editable input in the Service Basics grid, not the mock’s dashed read-only chip — chips can’t edit numbers and the primitives rule kept the existing control; convert if you prefer the chip look with an edit affordance. (2) New microcopy needing your review: the policy row’s no-minimum fallback “No weekly minimum · one-offs & approvals still allowed”, the preview’s “Group walks only” disabled caption, the “Not Offered” window flag pill, and the “New hidden rate option added to this concept.” notice. (3) The task prompt’s Zone Manager item was truncated mid-sentence; it was implemented as the Service Zones field opening the existing shared Zone Manager — flag if more was intended.
- **Walk Windows IA reconciliation (2026-08-15 — needs your sign-off before any relocation).** Two options for where Walk Windows lives relative to the `/portal/availability` calendar. **Option A (recommended):** Availability gains the same sub-tab pattern Business now uses — a Calendar tab (existing date-exception calendar) and a Walk Windows tab. Walk Windows, the Group Walk Policy card, and the Preview Client View drawer move there; Dog Walking under Business keeps Duration-Tiered Pricing plus its capacity inputs (Dogs Per Walker, Max Walks Per Day), which windows continue to read; both surfaces cross-link. Precedence rule stated on both surfaces: date exceptions always win over recurring windows — a Blocked date, or a Limited date with Dog Walking closed, suppresses every window that day. Rationale: “when walks happen” is availability semantics, walking configuration drops from four levels deep to two, and the spec itself frames Walk Windows under an Availability eyebrow. **Option B:** Walk Windows stays a Business tab and the Availability calendar gains a read-only recurring-windows summary strip linking into Business — preserves single-destination service configuration at the cost of Business staying denser. Nothing was relocated; awaiting your pick.
- **Per-service zones direction (2026-08-15).** Danny’s live feedback assumes separate zone sets per service (Walking now; In-Home and others later). The current mockup still shares one zone state between Business Profile’s Service Area and the Walking zones. Question: does Business Profile’s Service Area remain the business-wide base map with each service selecting/drawing its own subset, or does each service own a fully independent zone set? Affects the Zone Manager data model note for production.
- **Copy-doc reconciliation for the “MVP” removals (2026-08-15).** The public Pricing footnote edit (“in-app messaging arrives after launch”) diverges from the wording in the copy sources; the copy docs and COPY-AUDIT should be updated to lock the new phrasing.
- The July 18 copy document conflicts with later locked copy-audit decisions in several places. This build applies the later locked decisions and records every material change here.
- The spec originally called theme interactivity a follow-up, but Danny subsequently supplied a working Figma Make prototype. The interactive version is therefore retained as the newer direction.
- Support and Contact remain separate routes, as requested in the spec, even though both ultimately use `support@base509.com`.
- The policies frame has no separate mobile frame in the spec. Its mobile layout is a responsive derivation of desktop frame `2:138` using the same cards and copy.
- The portal has no Figma design by definition. Its information architecture and sample data are proposals for review, not approved production requirements.
- Production client management is proposed for the web portal: contact/pet records, invites, meet-and-greet state, exports, and account administration. Day-of-care operations and live booking execution remain native-app-first.
- The canonical `user_roles_and_permissions.md` defines Owner, Admin, Staff, and Client, so this pass removed the exploratory Manager option. Other planning/report documents still mention Manager; Product Management should reconcile that documentation before role enforcement is implemented.
- Stripe Connect, authentication, MFA/passkeys, invoice PDFs, server-generated exports, persistent settings, role enforcement, report queries, map provider selection, and real social-graphic storage still require backend product/architecture approval before implementation.
- Zone Manager is now one shared conceptual tool rather than separate maps per service. Danny selected Google Maps as the production map provider on 2026-08-15. The approved technical direction is Google Maps JavaScript + Terra Draw, with provider-neutral GeoJSON persisted per tenant. Production still needs a domain-restricted browser key, quota/billing alerts, address/geocoding authority, overlap validation, authorized server mutations, and RLS tests. Google’s removed Drawing Library must not be used.
- Reports are surfaced in this exploratory web portal. No report screen was added to the separate Expo/native project in accordance with the website/portal scope guardrail.
- Danny approved sharing the Availability calendar’s database and UX with the provider app. This pass builds only the web-portal concept; the separate Expo project still needs its platform-native calendar implementation. D-043 currently says all MVP configuration is web-only, so Product Management should update that decision if native availability editing is now part of MVP rather than a shared read-only view.
- Walk Windows is now surfaced in the exploratory Business Settings mockup, and the native client booking behavior is represented there as a static phone concept only. The separate Expo project still needs the production client flow; the drag-to-route group builder remains explicitly post-MVP.
- The Download Figma context exposes four provider screens. The repository currently contains two distinct, current client-side screen exports, so the Client Side tab uses those two real screens rather than inventing or duplicating additional mockups.

## Build / Test Runs

- `npm run build` — PASS after the functionality round (plan provider, plan-change flow, discount codes, Add Client, editable profile, Figma radios). All 54 static pages; green on every intermediate build.
- Functionality checks (in-app Chromium DOM measurement, 1440 × 900) — PASS. Plan change: Change Plan renders as a CTA button; the modal shows five selection radios (Enterprise = "Contact us"), Crew checked as Current plan; radios computed at 20px circles with 2px rgb(0,96,115) checked ring and 10px teal dot (Figma 247:16 spec); selecting Solo outlines 16 column cells and shows the proration copy ("17 days left … credited"); confirming updates the stat cards to Solo/$19 and cascades: Card (Stripe) row, Stripe Connect panel, and checkout Card method all disappear, the Business walking view drops the GPS chip and all walker machinery (single Dogs Per Walk field, no walker counts). Discount codes: lowercase "welcome20" applies as WELCOME20, Monthly Price and Next Renewal reprice to $63.20, the applied card shows with Remove. Clients: Add Client modal sends "Jamie Fox" to the Pending filter with the count moving 4 → 5 and the row in the roster. Marketing initials update to "MP" for "Marco Polo Care". Account profile fields are editable with Role locked; the MFA modal lists the four authenticator apps.
- Responsive spot checks — PASS at 390 × 844 for Account; Billing cannot be scrolled horizontally (see the measurement note above regarding the invoice table's internal scroll inflating the raw scrollWidth figure).
- `npm run build` — PASS after the commit-prep pass (Danny’s page edits, per-walker/per-window capacity, Google Maps integration, build-note removal, booking-rules boilerplate, step-circle sizing). All 54 static pages; run repeatedly through the pass, green every time.
- Commit-prep interaction checks (in-app Chromium DOM measurement, 1440 × 900) — PASS. Dashboard: Active Clients card opens “Newest Clients” with 4 rows and the clarifying caption; no build-note text anywhere on the page. Clients: 42 active rows render, the scroll box shows exactly 10 rows with a sticky header, caption “42 clients”; pending 4 / meet 3 match their cards. Reports: earnings/collected lines are cubic-Bézier paths with `pathLength` draw animation, comparison lines stay dashed, bars are rounded with staggered delays. Business: rate rows show the toggle (no pill) with per-type meta; Service Basics lists per-walker defaults (Morgan Lee 6, Casey Reed 4); Midday sums to 10 dogs; the capacity note reads “Morgan Lee 6 + Casey Reed 4 = 10 group dogs” with “Zone capacity from walker coverage: Mission 6 · Noe Valley 4 · Castro 6”; Morning’s per-window override input shows 4; Booking Rules textarea carries the 11-item boilerplate; step circles measure 36 × 36 with 16px digits. Zone Manager: without a Maps key the illustrated fallback renders with the key-required note and Draw Boundary present; the Google path was not exercisable (no key). Billing: Current Plan card and Change Plan action both open Compare Plans. Rendered pages contain no “MVP”, “concept”, “Exploratory”, “Prototype”, or “Stub” strings.
- Commit-prep responsive checks — PASS at 390 × 844: `scrollWidth === clientWidth` on Business (Dog Walking view), Clients, and Reports after the changes.
- Route smoke test — PASS: all eight portal routes return HTTP 200 on the dev server after the rebuild.
- `npm run build` — PASS after the Services & Pricing cleanup rebuild (sub-cards, connector strip, §2A conditional rendering, surcharge sub-card, CSS cleanup). Next.js compiled, type-checked, and generated all 54 static pages.
- Cleanup-rebuild structure check — PASS at 1440 × 900 (in-app Chromium DOM measurement; Playwright still not installed — same methodology note as below). Four sub-cards render in mock order with numbered badges, captions, and header actions (Service Active in ①; Add Rate Option in ②; Preview Client View + Add Window in ③; toggle + chevron on Surcharges); the old Pricing / Walk Windows tab pair is absent; the equation strip renders the mock copy with the group worked example; no horizontal overflow.
- Cleanup-rebuild rows check — PASS. Rate rows read “Individual · 30 min · $30 · +$10 extra dog · 1 household · Bookable” (Group rows with /dog and dogs/walker); window rows read “Midday · M–F · 10–2 · Individual + Group · 12 dogs · 2 walkers · Bookable”; Midday starts collapsed; expanding Group 30 shows the six existing editor fields with the Bookable switch and Duplicate/Remove in the footer and the chevron rotated 180°; the policy row is tinted, expands to the existing editor, and its summary updates live when Min Walks changes (3 → 5 → 3).
- ⓘ and Add checks — PASS. Both info toggles reveal the verbatim “Pricing and capacity stay separate.” and “How this works:” prose; Add Rate Option creates a hidden draft row already expanded with the status pill reading Hidden and a notice.
- §2A conditional-rendering check — PASS. Individual-only: connector example switches to the individual worked example; only Individual rates list; Group Walk Policy and Dogs Per Walker absent; window rows read “up to 12 walks / up to 6 walks”; restore notice reads “2 Group rates and 1 policy hidden — restored if you re-enable Group.”; the client preview omits the walk-type step and prices $30/walk. Group-only: only Group rates list with “2 Individual rates hidden — restored if you re-enable Individual.”; Morning (stored Individual-only) flags amber “Not Offered” with its dogs segment dropped. Both: full surface returns.
- Preview walk-type check — PASS. Under Both the drawer shows “1 · Individual or Group?” and renumbers the later steps 2/3/4; switching to Individual retitles to “Book Individual Walk”, enables Morning, removes spots pills, and reprices the CTA to $30/walk.
- Surcharge sub-card check — PASS. Header summary tracks state (“Puppy Handling · $10 · Boarding & Daycare”; adds “Boarding, Daycare & Dog Walking” when all three are checked); the chevron reveals the existing amount + Apply To controls.
- Service-off check — PASS. Service Active off fades and inerts the ① body and the strip + ② + ③ wrapper.
- Cleanup-rebuild responsive check — PASS at 390 × 844: `scrollWidth === clientWidth` on the walking surface, with a rate expanded, with a window expanded, and inside the open preview drawer; the equation strip stacks vertically; sub-card action buttons go full-width.
- Verification note: the browser pane’s screenshot capture glitched intermittently during this pass (blank frames); all checks above are DOM geometry/text assertions in the same viewport-resized Chromium, which were unaffected. Dev server restarted after the build (same `.next` clobber as earlier entries).
- `npm run build` — PASS after the density-pass feedback round (static header rows, pill removal, zone relocation, offering disclosure, service-off fade, MVP copy removal). Next.js compiled, type-checked, and generated all 54 static pages. Run three times across the day’s passes; all green.
- Density-pass verification note: Playwright is not installed in this repo or on this machine, and installing it would have added dependency changes to the intentionally dirty worktree. The same measurements (exact-viewport Chromium, `scrollWidth === clientWidth`, scripted DOM interaction) ran through the in-app Chromium browser instead; results below are from those runs.
- Density-pass interaction check — PASS at 1440 × 900. Four Business sub-tabs render and switch; scoped Saves emit tab-scoped notices (“Booking Rules saved in this static concept.”); the walking rate list renders collapsed one-line rows; expanding Group 30 opens the editor beneath a header row whose title, toggle, and chevron positions measure pixel-identical before and after (getBoundingClientRect deltas < 0.5px); the toggle in a collapsed row disables that option without expanding it; Walk Windows rows behave identically, with Window Name as the first field of the open body; Preview Client View opens the drawer, recurrence switches to Just This Week, the CTA reads “Request a Spot · M · W · F Midday — $22/walk”, and closing removes the drawer; the How-this-works hint expands with unchanged copy; the Service Zones trigger in walking general settings opens the shared Manage Service Zones modal.
- Progressive-disclosure check — PASS. Individual-only: Dogs Per Walker, Walk Type, capacity fields, Group Walk Policy, and per-window Services checkboxes all absent; every window summary reads “Individual only”; expanded rate shows only Duration / Base Rate / Holiday Rate / Extra Dog. Group-only: all rates read Group Walk and Dogs Per Walker stays. Both: full surface returns.
- Service-off check — PASS. Dog Walking’s Service Active off applies the fade class and `inert` to the whole body; Boarding’s toggle off sets the card inactive class and disables its price inputs; both restore on re-enable.
- Density-pass responsive check — PASS at 390 × 844: `scrollWidth === clientWidth` on all four Business sub-tabs, the collapsed and expanded rate list, the collapsed and expanded Walk Windows list, and inside the open preview drawer (full-width drawer, seven day chips fit). Rendered pages contain no “MVP” text at either viewport.
- `npm run build` — PASS after the two-tab Dog Walking configuration and shared Zone Manager integration. Next.js compiled, type-checked, and generated all 54 static pages; `/portal/business` is included.
- `npm run build` — PASS after Walk Windows, client booking, plan gating, and shared Chevron controls. Next.js compiled, type-checked, and generated all 54 static pages.
- Walking customization check — PASS. Add Rate Option still opens the editable walking-rate form. Changing Dogs Per Walker from 6 to 8 updates a two-walker window from 12 to 16 without adding a capacity input to the window.
- Walk Windows interaction check — PASS for the earlier plan-preview prototype; that provider-visible switch has since been removed and replaced by automatic subscription-state rendering.
- Shared Zone Manager follow-up — PASS. Business Profile and Dog Walking open the same Zone Manager state; adding “Bernal Heights” in Business Profile makes it selectable inside every Walk Window. Team-plan windows expose per-walker Coverage Zones, while no Solo / Duo & Up preview controls render.
- Dog Walking two-tab follow-up — PASS. Duration-Tiered Pricing retains the working Add Rate Option flow; Availability Walk Windows retains scheduling, group policy, client preview, and assignment controls. At 390 × 844 the compact Pricing / Walk Windows labels fit without scrolling, the Zone Manager modal fits at 342px, and the page reports no horizontal overflow.
- Client booking concept check — PASS. Dropping a Weekly selection below Min Walks / Week keeps “Request a Spot” available and exposes the provider-approval guidance; switching to Just This Week removes the recurring-minimum warning. Disabled Individual-only windows cannot be selected.
- Responsive Walk Windows check — PASS at desktop and 390 × 844. Provider cards and the client phone have no horizontal overflow; seven day chips remain inside the phone; recurrence controls stay on one line.
- Site-wide Chevron source/runtime audit — PASS. All PetAppro `select` elements compute to `appearance: none` with `icon-chevron-down.svg`; Service Area and Report navigation use the same exported icon. No text caret characters or runtime references to `icon-select-chevron.svg` remain in `apps/web/src`.
- `npm run build` — PASS after the multi-day Availability state model. Next.js compiled, type-checked, and generated all 54 static pages.
- Multi-day Availability interaction check — PASS. Individual date toggling produces a three-date consecutive range; Block All updates every selected date and reports the bulk action. Mobile visual QA at 390 × 844 confirms the smaller tiles, separated selection outlines, and `scrollWidth === clientWidth`.
- `npm run build` — PASS after the calendar color and Chevron asset pass. Next.js compiled, type-checked, and generated all 54 static pages.
- Calendar visual check — PASS. Booking badges and legend marker compute to Brand Accent green; holiday markers remain Brandy Blue. Previous/next controls both render `icon-chevron-down.svg` with directional transforms, and the rendered page contains no text caret characters.
- `npm run build` — PASS after the Availability calendar and explicit service-level holiday-rate pass. Next.js compiled, type-checked, and generated all 54 static pages, including `/portal/availability`.
- Availability browser check — PASS. The page opens on the next Woof-default holiday period; month navigation, custom holiday toggling, Open/Limited/Blocked states, service checkboxes, default reset, and staff notes update locally. Holiday pricing can coexist with limited availability, and the page has no horizontal overflow at the tested desktop viewport.
- Service holiday-rate check — PASS. The global Holiday Surcharge control is absent; Boarding and Daycare expose explicit holiday prices; all four initial walking options expose independent holiday rates; no horizontal page overflow was introduced.
- Earlier experimental build — PASS for the subsequently removed Holiday Surcharge card and inactive-control contrast pass. It generated all 53 pages before `/portal/availability` was added.
- `npm run build` — PASS after the Dog Walking, service-tab, Puppy Surcharge, and toggle-state pass. Next.js compiled, type-checked, and generated all 53 static pages; `/portal/business` is included in the production route manifest.
- Dog Walking browser interaction check — PASS. Service tabs retain a single 46px row at desktop, use the requested order, and have no horizontal page overflow. Individual/Group/Both filtering works; Group shows only Group rates; Add Rate Option opens with Group, Rate / Dog, and editable Dogs / Walker when Group is selected. Puppy Surcharge disabling fades and disables both the amount and service-applicability controls, and each service checkbox updates independently.
- Portal toggle visual check — PASS. Off-state tracks use the darker neutral inactive-control token; on-state is the darker green brand token. The repaired switch track measures 48 × 32px and is fully visible in both overview and rate cards.
- `npm run build` — PASS after the earlier portal feedback pass. Next.js compiled, type-checked, and generated 53 static pages. All eight marketing routes and the then-current seven portal concepts appeared in the route manifest.
- Local route-response smoke test — PASS. `/portal`, `/portal/clients`, `/portal/business`, `/portal/billing`, `/portal/account`, `/portal/marketing`, `/portal/reports`, `/petappro`, and `/petappro/pricing` each returned HTTP 200 from the development server on port 3003.
- Earlier browser smoke test at 390 × 844 — PASS for all eight marketing routes and the original five portal routes, with the expected H1 and `scrollWidth === clientWidth`.
- Earlier browser smoke test at 1440 × 1000 — PASS for all eight marketing routes and the original five portal routes, with the expected H1 and `scrollWidth === clientWidth`.
- Visual checks — PASS for Features, Pricing, Themes, Support, Policies, and every portal view at representative desktop/mobile sizes. The portal’s long business-setting values were retested after the mobile wrap fix.
- Portal feedback interaction checks — PASS. Manage Client exposes client/pet/history content; business initials update to `MP` for “Marco Polo Care”; Theme and Service Area overlays open; newly added staff expose Remove; Stripe guidance states that no keys are entered; Compare Plans includes prices; MFA setup opens; report filters/tabs/KPI overlays/date navigation and comparison legend update correctly.
- Pricing/code/security follow-up checks — PASS. Boarding shows the $5 Extended Rate Discount and 8+ day explanation with no shared Extended Care surcharge; Provider Code is read-only and described as server-assigned; the MFA overlay renders its QR enrollment preview; account deletion remains disabled until acknowledgment plus `DELETE`; all four Connect states update; and the full stubbed Before / Connect / After Stripe flow completes to Pending.
- Payment-method follow-up checks — PASS. Cash and payment apps start selected, Card starts Not Set Up, and the Connect module is faded plus disabled. Selecting Card activates all controls and adds Card to checkout. Deselecting payment apps removes the handle field and checkout label. Connect states map to Card chips as Pending / Active / Pending (Action Needed), while the detailed Connect module retains the full four-state wording.
- Portal mobile overflow check at 390 × 844 — PASS for Business and Reports (`scrollWidth === clientWidth`).
- Public plan-matrix visual check — PASS. Caption spacing is restored and every dark-header label uses inverse/white text.
- Theme interaction — PASS. Selecting South Bark Miami and Light updates both pressed states and the preview caption to “Previewing South Bark Miami · Light.”
- Feature carousel source/interaction audit — PASS. Four pagination buttons directly call `show(index)`, button pointer events are isolated from the swipe gesture surface, the fourth/tallest image controls the shared media-stage ratio, all graphics bottom-align, and both active/inactive dot transitions run for 300 ms unless reduced motion is requested.
- All-carousel audit — PASS. The only two true pagination systems are `FeatureSpotlightCarousel` and the shared `CardCarousel`; both expose one clickable button per screen/card and animate both directions of the active-state change. Theme palette dots are color swatches, not pagination, and Download screen rails do not display pagination dots.
- Support Chevron asset check — PASS. All four topic summaries render both exported SVGs and CSS displays Down when closed and Up when open; no text caret remains.
- Copy/guardrail scan — PASS for the page/component scope: no `type-body-sm` usage, Hollywood Olive references, old olive hex, competitor names, “Free forever,” “No per-staff fees,” or obsolete GPS v1.1 claims. The `type-body-sm` class definitions remain in the shared stylesheet but are unused on this website.
- Local development server: `http://localhost:3003`.

## Overnight Summary

Completed the remaining marketing routes from the specified Figma frames and canonical copy sources, with later locked decisions taking precedence where the sources disagree. The final feedback passes rebuilt Features around the current interactive carousel, added Download screen tabs, restored the full Support FAQ content, corrected Pricing/Themes mobile behavior, and applied the public-site card/text color rules. Expanded the separate exploratory provider portal to eight routes with detailed client management, business/theme/service-area configuration, explicit holiday rates and a Woof-derived availability calendar, canonical role presets, hosted Stripe onboarding and plan comparison concepts, MFA/passkey/session controls, local exports, reporting views, and a Figma-based social graphic generator. Production builds and responsive route/interaction checks pass. No commit, push, deploy, auth wiring, data wiring, or legal-body duplication was performed.

## Day Summary — 2026-08-15 (Cowork covering session for Codex)

What changed: the approved Business page density pass, plus Danny's same-day live feedback round. Business Settings is now four sub-tabs (Profile & Brand / Services & Pricing / Booking Rules / Team) with scoped Saves; walking rate cards and Walk Windows collapse to one-line summary rows whose title, toggle, and chevron stay pixel-static between states (the active toggle replaced the Bookable pill; renaming happens inside the open body); the client phone preview lives behind a Preview Client View drawer (new shared PortalDrawer); the teal explainers collapsed behind a shared PortalHint toggle; zone management moved from the configuration tabs up to a Service Zones control in walking general settings with per-service framing; the Individual/Group/Both offering now progressively discloses only relevant settings; inactive services fade and disable their bodies; and all customer-visible "MVP" mentions were removed.

What's verified: `npm run build` green all day (54 static pages every run); desktop 1440 × 900 interaction pass covering tabs, scoped saves, static-geometry expand/collapse, collapsed-row toggling, drawer, hints, zone modal, offering disclosure, and service-off fades; 390 × 844 responsive pass with `scrollWidth === clientWidth` across all sub-tabs, both lists in both states, and the open drawer. Measurements ran in exact-viewport Chromium via scripted DOM checks (Playwright is not installed in this repo; noted in Build / Test Runs).

What's next: Danny's sign-off on the Walk Windows IA (Option A: Availability sub-tabs Calendar + Walk Windows, recommended; Option B: stays in Business with a calendar summary strip); the per-service-zones data-model question; copy-doc reconciliation for the "arrives after launch" phrasing. No relocation happens before the sign-off.

Cumulative FILES TOUCHED today: `apps/web/src/components/PortalShell.tsx` · `apps/web/src/components/PortalBusinessView.tsx` · `apps/web/src/components/PortalWalkingRates.tsx` · `apps/web/src/components/PortalWalkWindows.tsx` · `apps/web/src/styles/brand-petappro.css` · `apps/web/src/app/petappro/pricing/page.tsx` · `docs/BUILD-LOG-overnight.md`

## Day Summary Addendum — 2026-08-15 (second Cowork session: Services & Pricing cleanup rebuild)

What changed: the Dog Walking tab was rebuilt to the approved services-cleanup mock — the Pricing / Walk Windows tab pair is gone, replaced by always-visible numbered sub-cards (① Service Basics with Service Active in its header, ② Pricing — Walking Rate Options, ③ Availability — Walk Windows with Group Walk Policy as its first tinted row) joined by the Rates × Windows = What-clients-can-book connector strip whose worked example follows the walk-type selection. Rows keep the existing editors on expand, show status pills at rest, and rotate a single chevron asset; Add Rate Option opens a new row expanded; "How this works" prose sits behind ⓘ icons; spec §2A drives hiding (never deleting) with quiet restore notices, walks-not-dogs capacity copy for Individual-only, and "Not Offered" flags under Group-only; the client preview adds a walk-type step only when Both is offered; Service Surcharges collapsed to a summary sub-card.

What's verified: `npm run build` green (54 pages); 1440 × 900 structure, rows, ⓘ, add-rate, §2A, preview, surcharge, and service-off checks; 390 × 844 responsive checks including the stacked equation strip and drawer — all via DOM measurement in the in-app Chromium (Playwright unavailable; pane screenshots glitched intermittently and were not relied on).

What's next: Danny's calls on the flagged details (Max Walks input vs chip, new microcopy, truncated Zone Manager instruction) plus the carried-over Walk Windows IA and per-service-zones questions.

Cumulative FILES TOUCHED today (both sessions): `apps/web/src/components/PortalShell.tsx` · `apps/web/src/components/PortalBusinessView.tsx` · `apps/web/src/components/PortalWalkingRates.tsx` · `apps/web/src/components/PortalWalkWindows.tsx` · `apps/web/src/styles/brand-petappro.css` · `apps/web/src/app/petappro/pricing/page.tsx` · `docs/BUILD-LOG-overnight.md`

## Commit Record — 2026-08-15

Portal concepts committed at Danny's direction as `cda4b78` (apps/web + this build log + the two portal design mocks; 181 files). The rest of the shared worktree remains uncommitted for its owners. Not pushed.

### Post-Commit Follow-ups — 2026-08-15 (same session)

- Dogs This Window is now a stepper per Danny: minus icon button · centered number field · plus icon button, each icon in a fully rounded 40px outlined circle with padding, wired to the per-window value with a floor of 1; the window total and zone-capacity line update live. `icon-plus.svg` came from Danny's export; `icon-minus.svg` was not present in the icon folder, so it was derived from the plus asset's exact bar geometry (same fill, caps, and 24-grid) — swap it if Danny exports an official one.
- Danny's booking-enforcement question (individual-only windows) answered in-session and recorded here: correct — when a window accepts only Individual, booking enforcement (one household per slot, sequenced with the Travel Buffer, bounded by Max Walks Per Day / Walker) belongs to the booking engine (`packages/booking`, planned; capacity-model §6B contract), not the config UI. The portal captures the config; the app's booking flow enforces it at request/approval time. Flagged for the booking-engine spec pass: make individual-walk sequencing with travel buffers explicit.
- FILES TOUCHED (follow-up): `apps/web/src/components/PortalWalkWindows.tsx` · `apps/web/src/styles/brand-petappro.css` · `apps/web/public/brands/petappro.com/icon-plus.svg` (Danny) · `apps/web/public/brands/petappro.com/icon-minus.svg` (derived) · `docs/BUILD-LOG-overnight.md`
